The bearoff program generates the bearoff database in the form of
a C++ structure that is in turn included and compiled by the main
code.

It would be cleaner to simply build a library with one entry point.
